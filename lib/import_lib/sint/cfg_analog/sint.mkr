(marker_file
 (version 1.0)
 (markers
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 40)
   (error_num 1)
   (short_msg "ERROR : Property LOCATION on I217(page3) has same value as property LOCATION on I178(page3)")
   (long_msg "
First Instance : I217(page3)
Property Name  : LOCATION
Second Instance: I178(page3)
Property Name  : LOCATION
Property LOCATION on I217(page3) has identical value as property LOCATION on I178(page3).
This is incorrect.
LOCATION/REFDES property, if specified should be unique.

Please assign unique LOCATION/REFDES property values to the instances.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i178;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 2)
   (short_msg "WARNING : Hierarchical block page3_i115 is empty")
   (long_msg "
Hierarchical block instance: I115(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i115;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 3)
   (short_msg "WARNING : Hierarchical block page3_i99 is empty")
   (long_msg "
Hierarchical block instance: I99(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i99;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 4)
   (short_msg "WARNING : Hierarchical block page3_i42 is empty")
   (long_msg "
Hierarchical block instance: I42(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i42;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 5)
   (short_msg "WARNING : Hierarchical block page2_i521 is empty")
   (long_msg "
Hierarchical block instance: I521(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i521;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 6)
   (short_msg "WARNING : Hierarchical block page2_i492 is empty")
   (long_msg "
Hierarchical block instance: I492(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i492;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 7)
   (short_msg "WARNING : Hierarchical block page2_i327 is empty")
   (long_msg "
Hierarchical block instance: I327(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i327;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 8)
   (short_msg "INFO : Unconnected pin 'e_OP_21_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 9)
   (short_msg "INFO : Unconnected pin 'd_OP_13_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 10)
   (short_msg "INFO : Unconnected pin 'e_OP_4_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 11)
   (short_msg "INFO : Unconnected pin 'c_OP_21_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 12)
   (short_msg "INFO : Unconnected pin 'e_OP_16_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 13)
   (short_msg "INFO : Unconnected pin 'c_OP_6_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 14)
   (short_msg "INFO : Unconnected pin 'a_OP_21_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 15)
   (short_msg "INFO : Unconnected pin 'f_OP_6_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 16)
   (short_msg "INFO : Unconnected pin 'c_OP_16_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 17)
   (short_msg "INFO : Unconnected pin 'a_OP_8_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 18)
   (short_msg "INFO : Unconnected pin 'f_OP_19_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 19)
   (short_msg "INFO : Unconnected pin 'd_OP_8_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 20)
   (short_msg "INFO : Unconnected pin 'a_OP_16_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 21)
   (short_msg "INFO : Unconnected pin 'd_OP_19_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 22)
   (short_msg "INFO : Unconnected pin 'a_OP_1_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 23)
   (short_msg "INFO : Unconnected pin 'f_OP_12_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 24)
   (short_msg "INFO : Unconnected pin 'd_OP_1_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 25)
   (short_msg "INFO : Unconnected pin 'e_OP_20_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 26)
   (short_msg "INFO : Unconnected pin 'd_OP_12_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 27)
   (short_msg "INFO : Unconnected pin 'e_OP_3_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 28)
   (short_msg "INFO : Unconnected pin 'c_OP_20_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 29)
   (short_msg "INFO : Unconnected pin 'e_OP_15_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 30)
   (short_msg "INFO : Unconnected pin 'c_OP_5_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 31)
   (short_msg "INFO : Unconnected pin 'a_OP_20_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 32)
   (short_msg "INFO : Unconnected pin 'f_OP_5_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 33)
   (short_msg "INFO : Unconnected pin 'c_OP_15_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 34)
   (short_msg "INFO : Unconnected pin 'a_OP_7_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 35)
   (short_msg "INFO : Unconnected pin 'f_OP_18_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 36)
   (short_msg "INFO : Unconnected pin 'd_OP_7_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 37)
   (short_msg "INFO : Unconnected pin 'a_OP_15_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 38)
   (short_msg "INFO : Unconnected pin 'd_OP_18_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 39)
   (short_msg "INFO : Unconnected pin 'e_OP_9_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 40)
   (short_msg "INFO : Unconnected pin 'f_OP_11_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 41)
   (short_msg "INFO : Unconnected pin 'd_OP_11_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 42)
   (short_msg "INFO : Unconnected pin 'e_OP_2_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 43)
   (short_msg "INFO : Unconnected pin 'f_OP_22_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 44)
   (short_msg "INFO : Unconnected pin 'e_OP_14_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 45)
   (short_msg "INFO : Unconnected pin 'c_OP_4_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 46)
   (short_msg "INFO : Unconnected pin 'f_OP_4_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 47)
   (short_msg "INFO : Unconnected pin 'd_OP_22_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 48)
   (short_msg "INFO : Unconnected pin 'c_OP_14_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 49)
   (short_msg "INFO : Unconnected pin 'a_OP_6_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 50)
   (short_msg "INFO : Unconnected pin 'f_OP_17_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 51)
   (short_msg "INFO : Unconnected pin 'd_OP_6_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 52)
   (short_msg "INFO : Unconnected pin 'a_OP_14_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 53)
   (short_msg "INFO : Unconnected pin 'd_OP_17_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 54)
   (short_msg "INFO : Unconnected pin 'e_OP_8_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 55)
   (short_msg "INFO : Unconnected pin 'f_OP_10_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 56)
   (short_msg "INFO : Unconnected pin 'd_OP_10_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 57)
   (short_msg "INFO : Unconnected pin 'e_OP_1_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 58)
   (short_msg "INFO : Unconnected pin 'f_OP_21_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 59)
   (short_msg "INFO : Unconnected pin 'e_OP_13_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 60)
   (short_msg "INFO : Unconnected pin 'c_OP_3_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 61)
   (short_msg "INFO : Unconnected pin 'f_OP_3_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 62)
   (short_msg "INFO : Unconnected pin 'd_OP_21_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 63)
   (short_msg "INFO : Unconnected pin 'c_OP_13_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 64)
   (short_msg "INFO : Unconnected pin 'a_OP_5_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 65)
   (short_msg "INFO : Unconnected pin 'f_OP_16_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 66)
   (short_msg "INFO : Unconnected pin 'd_OP_5_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 67)
   (short_msg "INFO : Unconnected pin 'a_OP_13_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 68)
   (short_msg "INFO : Unconnected pin 'd_OP_16_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 69)
   (short_msg "INFO : Unconnected pin 'e_OP_7_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 70)
   (short_msg "INFO : Unconnected pin 'e_OP_19_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 71)
   (short_msg "INFO : Unconnected pin 'c_OP_9_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 72)
   (short_msg "INFO : Unconnected pin 'f_OP_9_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 73)
   (short_msg "INFO : Unconnected pin 'c_OP_19_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 74)
   (short_msg "INFO : Unconnected pin 'a_OP_19_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 75)
   (short_msg "INFO : Unconnected pin 'f_OP_20_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 76)
   (short_msg "INFO : Unconnected pin 'e_OP_12_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 77)
   (short_msg "INFO : Unconnected pin 'c_OP_2_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 78)
   (short_msg "INFO : Unconnected pin 'f_OP_2_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 79)
   (short_msg "INFO : Unconnected pin 'd_OP_20_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 80)
   (short_msg "INFO : Unconnected pin 'c_OP_12_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 81)
   (short_msg "INFO : Unconnected pin 'a_OP_4_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 82)
   (short_msg "INFO : Unconnected pin 'f_OP_15_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 83)
   (short_msg "INFO : Unconnected pin 'd_OP_4_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 84)
   (short_msg "INFO : Unconnected pin 'a_OP_12_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 85)
   (short_msg "INFO : Unconnected pin 'd_OP_15_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 86)
   (short_msg "INFO : Unconnected pin 'e_OP_6_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 87)
   (short_msg "INFO : Unconnected pin 'e_OP_18_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 88)
   (short_msg "INFO : Unconnected pin 'c_OP_8_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 89)
   (short_msg "INFO : Unconnected pin 'f_OP_8_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 90)
   (short_msg "INFO : Unconnected pin 'c_OP_18_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 91)
   (short_msg "INFO : Unconnected pin 'a_OP_18_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 92)
   (short_msg "INFO : Unconnected pin 'e_OP_11_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 93)
   (short_msg "INFO : Unconnected pin 'c_OP_1_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 94)
   (short_msg "INFO : Unconnected pin 'f_OP_1_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 95)
   (short_msg "INFO : Unconnected pin 'c_OP_11_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 96)
   (short_msg "INFO : Unconnected pin 'a_OP_3_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 97)
   (short_msg "INFO : Unconnected pin 'f_OP_14_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 98)
   (short_msg "INFO : Unconnected pin 'd_OP_3_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 99)
   (short_msg "INFO : Unconnected pin 'a_OP_11_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 100)
   (short_msg "INFO : Unconnected pin 'e_OP_22_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 101)
   (short_msg "INFO : Unconnected pin 'd_OP_14_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 102)
   (short_msg "INFO : Unconnected pin 'e_OP_5_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 103)
   (short_msg "INFO : Unconnected pin 'c_OP_22_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 104)
   (short_msg "INFO : Unconnected pin 'e_OP_17_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 105)
   (short_msg "INFO : Unconnected pin 'c_OP_7_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 106)
   (short_msg "INFO : Unconnected pin 'a_OP_22_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 107)
   (short_msg "INFO : Unconnected pin 'f_OP_7_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 108)
   (short_msg "INFO : Unconnected pin 'c_OP_17_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 109)
   (short_msg "INFO : Unconnected pin 'a_OP_9_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 110)
   (short_msg "INFO : Unconnected pin 'd_OP_9_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 111)
   (short_msg "INFO : Unconnected pin 'a_OP_17_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 112)
   (short_msg "INFO : Unconnected pin 'e_OP_10_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: e_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.e_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 113)
   (short_msg "INFO : Unconnected pin 'c_OP_10_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: c_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.c_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 114)
   (short_msg "INFO : Unconnected pin 'a_OP_2_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 115)
   (short_msg "INFO : Unconnected pin 'f_OP_13_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: f_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.f_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 116)
   (short_msg "INFO : Unconnected pin 'd_OP_2_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: d_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.d_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 117)
   (short_msg "INFO : Unconnected pin 'a_OP_10_CP_' on hierarchical block page3_i217")
   (long_msg "
Instance: I217(page3)
Pin Name: a_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i217.a_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 118)
   (short_msg "WARNING : Hierarchical block page3_i217 is empty")
   (long_msg "
Hierarchical block instance: I217(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i217;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 119)
   (short_msg "INFO : Unconnected pin 'e_OP_21_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 120)
   (short_msg "INFO : Unconnected pin 'b_OP_4_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 121)
   (short_msg "INFO : Unconnected pin 'e_OP_4_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 122)
   (short_msg "INFO : Unconnected pin 'c_OP_21_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 123)
   (short_msg "INFO : Unconnected pin 'b_OP_13_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 124)
   (short_msg "INFO : Unconnected pin 'e_OP_16_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 125)
   (short_msg "INFO : Unconnected pin 'c_OP_6_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 126)
   (short_msg "INFO : Unconnected pin 'a_OP_21_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 127)
   (short_msg "INFO : Unconnected pin 'f_OP_6_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 128)
   (short_msg "INFO : Unconnected pin 'c_OP_16_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 129)
   (short_msg "INFO : Unconnected pin 'a_OP_8_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 130)
   (short_msg "INFO : Unconnected pin 'f_OP_19_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 131)
   (short_msg "INFO : Unconnected pin 'a_OP_16_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 132)
   (short_msg "INFO : Unconnected pin 'b_OP_19_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 133)
   (short_msg "INFO : Unconnected pin 'a_OP_1_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 134)
   (short_msg "INFO : Unconnected pin 'f_OP_12_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 135)
   (short_msg "INFO : Unconnected pin 'e_OP_20_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 136)
   (short_msg "INFO : Unconnected pin 'b_OP_3_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 137)
   (short_msg "INFO : Unconnected pin 'e_OP_3_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 138)
   (short_msg "INFO : Unconnected pin 'c_OP_20_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 139)
   (short_msg "INFO : Unconnected pin 'b_OP_12_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 140)
   (short_msg "INFO : Unconnected pin 'e_OP_15_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 141)
   (short_msg "INFO : Unconnected pin 'c_OP_5_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 142)
   (short_msg "INFO : Unconnected pin 'a_OP_20_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 143)
   (short_msg "INFO : Unconnected pin 'f_OP_5_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 144)
   (short_msg "INFO : Unconnected pin 'c_OP_15_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 145)
   (short_msg "INFO : Unconnected pin 'a_OP_7_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 146)
   (short_msg "INFO : Unconnected pin 'f_OP_18_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 147)
   (short_msg "INFO : Unconnected pin 'a_OP_15_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 148)
   (short_msg "INFO : Unconnected pin 'b_OP_9_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 149)
   (short_msg "INFO : Unconnected pin 'e_OP_9_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 150)
   (short_msg "INFO : Unconnected pin 'b_OP_18_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 151)
   (short_msg "INFO : Unconnected pin 'f_OP_11_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 152)
   (short_msg "INFO : Unconnected pin 'b_OP_2_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 153)
   (short_msg "INFO : Unconnected pin 'e_OP_2_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 154)
   (short_msg "INFO : Unconnected pin 'b_OP_11_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 155)
   (short_msg "INFO : Unconnected pin 'f_OP_22_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 156)
   (short_msg "INFO : Unconnected pin 'e_OP_14_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 157)
   (short_msg "INFO : Unconnected pin 'c_OP_4_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 158)
   (short_msg "INFO : Unconnected pin 'f_OP_4_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 159)
   (short_msg "INFO : Unconnected pin 'c_OP_14_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 160)
   (short_msg "INFO : Unconnected pin 'a_OP_6_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 161)
   (short_msg "INFO : Unconnected pin 'f_OP_17_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 162)
   (short_msg "INFO : Unconnected pin 'b_OP_22_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 163)
   (short_msg "INFO : Unconnected pin 'a_OP_14_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 164)
   (short_msg "INFO : Unconnected pin 'b_OP_8_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 165)
   (short_msg "INFO : Unconnected pin 'e_OP_8_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 166)
   (short_msg "INFO : Unconnected pin 'b_OP_17_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 167)
   (short_msg "INFO : Unconnected pin 'f_OP_10_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 168)
   (short_msg "INFO : Unconnected pin 'b_OP_1_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 169)
   (short_msg "INFO : Unconnected pin 'e_OP_1_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 170)
   (short_msg "INFO : Unconnected pin 'b_OP_10_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 171)
   (short_msg "INFO : Unconnected pin 'f_OP_21_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 172)
   (short_msg "INFO : Unconnected pin 'e_OP_13_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 173)
   (short_msg "INFO : Unconnected pin 'c_OP_3_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 174)
   (short_msg "INFO : Unconnected pin 'f_OP_3_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 175)
   (short_msg "INFO : Unconnected pin 'c_OP_13_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 176)
   (short_msg "INFO : Unconnected pin 'a_OP_5_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 177)
   (short_msg "INFO : Unconnected pin 'f_OP_16_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 178)
   (short_msg "INFO : Unconnected pin 'b_OP_21_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 179)
   (short_msg "INFO : Unconnected pin 'a_OP_13_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 180)
   (short_msg "INFO : Unconnected pin 'b_OP_7_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 181)
   (short_msg "INFO : Unconnected pin 'e_OP_7_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 182)
   (short_msg "INFO : Unconnected pin 'b_OP_16_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 183)
   (short_msg "INFO : Unconnected pin 'e_OP_19_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 184)
   (short_msg "INFO : Unconnected pin 'c_OP_9_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 185)
   (short_msg "INFO : Unconnected pin 'f_OP_9_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 186)
   (short_msg "INFO : Unconnected pin 'c_OP_19_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 187)
   (short_msg "INFO : Unconnected pin 'a_OP_19_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 188)
   (short_msg "INFO : Unconnected pin 'f_OP_20_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 189)
   (short_msg "INFO : Unconnected pin 'e_OP_12_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 190)
   (short_msg "INFO : Unconnected pin 'c_OP_2_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 191)
   (short_msg "INFO : Unconnected pin 'f_OP_2_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 192)
   (short_msg "INFO : Unconnected pin 'c_OP_12_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 193)
   (short_msg "INFO : Unconnected pin 'a_OP_4_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 194)
   (short_msg "INFO : Unconnected pin 'f_OP_15_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 195)
   (short_msg "INFO : Unconnected pin 'b_OP_20_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 196)
   (short_msg "INFO : Unconnected pin 'a_OP_12_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 197)
   (short_msg "INFO : Unconnected pin 'b_OP_6_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 198)
   (short_msg "INFO : Unconnected pin 'e_OP_6_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 199)
   (short_msg "INFO : Unconnected pin 'b_OP_15_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 200)
   (short_msg "INFO : Unconnected pin 'e_OP_18_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 201)
   (short_msg "INFO : Unconnected pin 'c_OP_8_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 202)
   (short_msg "INFO : Unconnected pin 'f_OP_8_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 203)
   (short_msg "INFO : Unconnected pin 'c_OP_18_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 204)
   (short_msg "INFO : Unconnected pin 'a_OP_18_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 205)
   (short_msg "INFO : Unconnected pin 'e_OP_11_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 206)
   (short_msg "INFO : Unconnected pin 'c_OP_1_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 207)
   (short_msg "INFO : Unconnected pin 'f_OP_1_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 208)
   (short_msg "INFO : Unconnected pin 'c_OP_11_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 209)
   (short_msg "INFO : Unconnected pin 'a_OP_3_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 210)
   (short_msg "INFO : Unconnected pin 'f_OP_14_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 211)
   (short_msg "INFO : Unconnected pin 'a_OP_11_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 212)
   (short_msg "INFO : Unconnected pin 'e_OP_22_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 213)
   (short_msg "INFO : Unconnected pin 'b_OP_5_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 214)
   (short_msg "INFO : Unconnected pin 'e_OP_5_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 215)
   (short_msg "INFO : Unconnected pin 'c_OP_22_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 216)
   (short_msg "INFO : Unconnected pin 'b_OP_14_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: b_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.b_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 217)
   (short_msg "INFO : Unconnected pin 'e_OP_17_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 218)
   (short_msg "INFO : Unconnected pin 'c_OP_7_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 219)
   (short_msg "INFO : Unconnected pin 'a_OP_22_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 220)
   (short_msg "INFO : Unconnected pin 'f_OP_7_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 221)
   (short_msg "INFO : Unconnected pin 'c_OP_17_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 222)
   (short_msg "INFO : Unconnected pin 'a_OP_9_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 223)
   (short_msg "INFO : Unconnected pin 'a_OP_17_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 224)
   (short_msg "INFO : Unconnected pin 'e_OP_10_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: e_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.e_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 225)
   (short_msg "INFO : Unconnected pin 'c_OP_10_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: c_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.c_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 226)
   (short_msg "INFO : Unconnected pin 'a_OP_2_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 227)
   (short_msg "INFO : Unconnected pin 'f_OP_13_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: f_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.f_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 228)
   (short_msg "INFO : Unconnected pin 'a_OP_10_CP_' on hierarchical block page3_i177")
   (long_msg "
Instance: I177(page3)
Pin Name: a_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i177.a_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 229)
   (short_msg "WARNING : Hierarchical block page3_i177 is empty")
   (long_msg "
Hierarchical block instance: I177(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i177;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 230)
   (short_msg "WARNING : Hierarchical block page3_i110 is empty")
   (long_msg "
Hierarchical block instance: I110(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i110;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 231)
   (short_msg "WARNING : Hierarchical block page2_i594 is empty")
   (long_msg "
Hierarchical block instance: I594(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i594;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 232)
   (short_msg "WARNING : Hierarchical block page2_i404 is empty")
   (long_msg "
Hierarchical block instance: I404(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i404;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 233)
   (short_msg "WARNING : Hierarchical block page2_i580 is empty")
   (long_msg "
Hierarchical block instance: I580(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i580;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 234)
   (short_msg "WARNING : Hierarchical block page2_i326 is empty")
   (long_msg "
Hierarchical block instance: I326(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i326;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 235)
   (short_msg "WARNING : Hierarchical block page2_i322 is empty")
   (long_msg "
Hierarchical block instance: I322(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i322;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 236)
   (short_msg "WARNING : Hierarchical block page3_i125 is empty")
   (long_msg "
Hierarchical block instance: I125(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i125;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 237)
   (short_msg "WARNING : Hierarchical block page2_i547 is empty")
   (long_msg "
Hierarchical block instance: I547(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i547;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 238)
   (short_msg "WARNING : Hierarchical block page2_i508 is empty")
   (long_msg "
Hierarchical block instance: I508(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i508;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 239)
   (short_msg "WARNING : Hierarchical block page2_i498 is empty")
   (long_msg "
Hierarchical block instance: I498(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i498;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 240)
   (short_msg "WARNING : Hierarchical block page3_i120 is empty")
   (long_msg "
Hierarchical block instance: I120(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i120;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 241)
   (short_msg "WARNING : Hierarchical block page3_i66 is empty")
   (long_msg "
Hierarchical block instance: I66(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i66;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 242)
   (short_msg "WARNING : Hierarchical block page2_i86 is empty")
   (long_msg "
Hierarchical block instance: I86(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i86;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 243)
   (short_msg "WARNING : Hierarchical block page3_i160 is empty")
   (long_msg "
Hierarchical block instance: I160(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i160;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 244)
   (short_msg "WARNING : Hierarchical block page2_i406 is empty")
   (long_msg "
Hierarchical block instance: I406(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i406;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 245)
   (short_msg "WARNING : Hierarchical block page3_i193 is empty")
   (long_msg "
Hierarchical block instance: I193(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i193;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 246)
   (short_msg "INFO : Unconnected pin 'e_OP_21_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 247)
   (short_msg "INFO : Unconnected pin 'd_OP_13_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 248)
   (short_msg "INFO : Unconnected pin 'b_OP_4_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 249)
   (short_msg "INFO : Unconnected pin 'e_OP_4_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 250)
   (short_msg "INFO : Unconnected pin 'c_OP_21_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 251)
   (short_msg "INFO : Unconnected pin 'b_OP_13_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 252)
   (short_msg "INFO : Unconnected pin 'e_OP_16_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 253)
   (short_msg "INFO : Unconnected pin 'c_OP_6_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 254)
   (short_msg "INFO : Unconnected pin 'f_OP_6_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 255)
   (short_msg "INFO : Unconnected pin 'c_OP_16_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 256)
   (short_msg "INFO : Unconnected pin 'f_OP_19_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 257)
   (short_msg "INFO : Unconnected pin 'd_OP_8_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 258)
   (short_msg "INFO : Unconnected pin 'd_OP_19_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 259)
   (short_msg "INFO : Unconnected pin 'b_OP_19_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 260)
   (short_msg "INFO : Unconnected pin 'f_OP_12_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 261)
   (short_msg "INFO : Unconnected pin 'd_OP_1_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 262)
   (short_msg "INFO : Unconnected pin 'e_OP_20_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 263)
   (short_msg "INFO : Unconnected pin 'd_OP_12_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 264)
   (short_msg "INFO : Unconnected pin 'b_OP_3_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 265)
   (short_msg "INFO : Unconnected pin 'e_OP_3_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 266)
   (short_msg "INFO : Unconnected pin 'c_OP_20_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 267)
   (short_msg "INFO : Unconnected pin 'b_OP_12_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 268)
   (short_msg "INFO : Unconnected pin 'e_OP_15_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 269)
   (short_msg "INFO : Unconnected pin 'c_OP_5_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 270)
   (short_msg "INFO : Unconnected pin 'f_OP_5_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 271)
   (short_msg "INFO : Unconnected pin 'c_OP_15_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 272)
   (short_msg "INFO : Unconnected pin 'f_OP_18_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 273)
   (short_msg "INFO : Unconnected pin 'd_OP_7_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 274)
   (short_msg "INFO : Unconnected pin 'd_OP_18_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 275)
   (short_msg "INFO : Unconnected pin 'b_OP_9_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 276)
   (short_msg "INFO : Unconnected pin 'e_OP_9_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 277)
   (short_msg "INFO : Unconnected pin 'b_OP_18_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 278)
   (short_msg "INFO : Unconnected pin 'f_OP_11_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 279)
   (short_msg "INFO : Unconnected pin 'd_OP_11_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 280)
   (short_msg "INFO : Unconnected pin 'b_OP_2_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 281)
   (short_msg "INFO : Unconnected pin 'e_OP_2_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 282)
   (short_msg "INFO : Unconnected pin 'b_OP_11_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 283)
   (short_msg "INFO : Unconnected pin 'f_OP_22_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 284)
   (short_msg "INFO : Unconnected pin 'e_OP_14_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 285)
   (short_msg "INFO : Unconnected pin 'c_OP_4_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 286)
   (short_msg "INFO : Unconnected pin 'f_OP_4_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 287)
   (short_msg "INFO : Unconnected pin 'd_OP_22_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 288)
   (short_msg "INFO : Unconnected pin 'c_OP_14_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 289)
   (short_msg "INFO : Unconnected pin 'f_OP_17_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 290)
   (short_msg "INFO : Unconnected pin 'd_OP_6_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 291)
   (short_msg "INFO : Unconnected pin 'b_OP_22_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 292)
   (short_msg "INFO : Unconnected pin 'd_OP_17_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 293)
   (short_msg "INFO : Unconnected pin 'b_OP_8_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 294)
   (short_msg "INFO : Unconnected pin 'e_OP_8_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 295)
   (short_msg "INFO : Unconnected pin 'b_OP_17_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 296)
   (short_msg "INFO : Unconnected pin 'f_OP_10_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 297)
   (short_msg "INFO : Unconnected pin 'd_OP_10_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 298)
   (short_msg "INFO : Unconnected pin 'b_OP_1_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 299)
   (short_msg "INFO : Unconnected pin 'e_OP_1_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 300)
   (short_msg "INFO : Unconnected pin 'b_OP_10_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 301)
   (short_msg "INFO : Unconnected pin 'f_OP_21_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 302)
   (short_msg "INFO : Unconnected pin 'e_OP_13_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 303)
   (short_msg "INFO : Unconnected pin 'c_OP_3_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 304)
   (short_msg "INFO : Unconnected pin 'f_OP_3_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 305)
   (short_msg "INFO : Unconnected pin 'd_OP_21_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 306)
   (short_msg "INFO : Unconnected pin 'c_OP_13_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 307)
   (short_msg "INFO : Unconnected pin 'f_OP_16_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 308)
   (short_msg "INFO : Unconnected pin 'd_OP_5_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 309)
   (short_msg "INFO : Unconnected pin 'b_OP_21_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 310)
   (short_msg "INFO : Unconnected pin 'd_OP_16_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 311)
   (short_msg "INFO : Unconnected pin 'b_OP_7_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 312)
   (short_msg "INFO : Unconnected pin 'e_OP_7_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 313)
   (short_msg "INFO : Unconnected pin 'b_OP_16_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 314)
   (short_msg "INFO : Unconnected pin 'e_OP_19_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 315)
   (short_msg "INFO : Unconnected pin 'c_OP_9_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 316)
   (short_msg "INFO : Unconnected pin 'f_OP_9_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 317)
   (short_msg "INFO : Unconnected pin 'c_OP_19_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 318)
   (short_msg "INFO : Unconnected pin 'f_OP_20_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 319)
   (short_msg "INFO : Unconnected pin 'e_OP_12_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 320)
   (short_msg "INFO : Unconnected pin 'c_OP_2_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 321)
   (short_msg "INFO : Unconnected pin 'f_OP_2_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 322)
   (short_msg "INFO : Unconnected pin 'd_OP_20_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 323)
   (short_msg "INFO : Unconnected pin 'c_OP_12_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 324)
   (short_msg "INFO : Unconnected pin 'f_OP_15_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 325)
   (short_msg "INFO : Unconnected pin 'd_OP_4_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 326)
   (short_msg "INFO : Unconnected pin 'b_OP_20_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 327)
   (short_msg "INFO : Unconnected pin 'd_OP_15_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 328)
   (short_msg "INFO : Unconnected pin 'b_OP_6_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 329)
   (short_msg "INFO : Unconnected pin 'e_OP_6_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 330)
   (short_msg "INFO : Unconnected pin 'b_OP_15_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 331)
   (short_msg "INFO : Unconnected pin 'e_OP_18_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 332)
   (short_msg "INFO : Unconnected pin 'c_OP_8_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 333)
   (short_msg "INFO : Unconnected pin 'f_OP_8_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 334)
   (short_msg "INFO : Unconnected pin 'c_OP_18_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 335)
   (short_msg "INFO : Unconnected pin 'e_OP_11_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 336)
   (short_msg "INFO : Unconnected pin 'c_OP_1_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 337)
   (short_msg "INFO : Unconnected pin 'f_OP_1_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 338)
   (short_msg "INFO : Unconnected pin 'c_OP_11_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 339)
   (short_msg "INFO : Unconnected pin 'f_OP_14_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 340)
   (short_msg "INFO : Unconnected pin 'd_OP_3_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 341)
   (short_msg "INFO : Unconnected pin 'e_OP_22_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 342)
   (short_msg "INFO : Unconnected pin 'd_OP_14_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 343)
   (short_msg "INFO : Unconnected pin 'b_OP_5_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 344)
   (short_msg "INFO : Unconnected pin 'e_OP_5_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 345)
   (short_msg "INFO : Unconnected pin 'c_OP_22_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 346)
   (short_msg "INFO : Unconnected pin 'b_OP_14_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: b_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.b_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 347)
   (short_msg "INFO : Unconnected pin 'e_OP_17_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 348)
   (short_msg "INFO : Unconnected pin 'c_OP_7_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 349)
   (short_msg "INFO : Unconnected pin 'f_OP_7_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 350)
   (short_msg "INFO : Unconnected pin 'c_OP_17_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 351)
   (short_msg "INFO : Unconnected pin 'd_OP_9_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 352)
   (short_msg "INFO : Unconnected pin 'e_OP_10_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: e_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.e_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 353)
   (short_msg "INFO : Unconnected pin 'c_OP_10_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: c_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.c_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 354)
   (short_msg "INFO : Unconnected pin 'f_OP_13_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: f_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.f_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 355)
   (short_msg "INFO : Unconnected pin 'd_OP_2_CP_' on hierarchical block page3_i178")
   (long_msg "
Instance: I178(page3)
Pin Name: d_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i178.d_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 356)
   (short_msg "WARNING : Hierarchical block page3_i178 is empty")
   (long_msg "
Hierarchical block instance: I178(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i178;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 357)
   (short_msg "WARNING : Hierarchical block page3_i80 is empty")
   (long_msg "
Hierarchical block instance: I80(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i80;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 358)
   (short_msg "WARNING : Hierarchical block page3_i47 is empty")
   (long_msg "
Hierarchical block instance: I47(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i47;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 359)
   (short_msg "INFO : Unconnected pin 'i/o_82' on hierarchical block page2_i616")
   (long_msg "
Instance: I616(page2)
Pin Name: i/o_82

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i616.i/o_82;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 360)
   (short_msg "INFO : Unconnected pin 'i/o_81' on hierarchical block page2_i616")
   (long_msg "
Instance: I616(page2)
Pin Name: i/o_81

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i616.i/o_81;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 361)
   (short_msg "INFO : Unconnected pin 'i/o_80' on hierarchical block page2_i616")
   (long_msg "
Instance: I616(page2)
Pin Name: i/o_80

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i616.i/o_80;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 362)
   (short_msg "WARNING : Hierarchical block page2_i616 is empty")
   (long_msg "
Hierarchical block instance: I616(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i616;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 363)
   (short_msg "WARNING : Hierarchical block page2_i595 is empty")
   (long_msg "
Hierarchical block instance: I595(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i595;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 364)
   (short_msg "WARNING : Hierarchical block page2_i434 is empty")
   (long_msg "
Hierarchical block instance: I434(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i434;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 365)
   (short_msg "INFO : Unconnected pin 'x2' on hierarchical block page2_i393")
   (long_msg "
Instance: I393(page2)
Pin Name: x2

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i393.x2;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 366)
   (short_msg "WARNING : Hierarchical block page2_i393 is empty")
   (long_msg "
Hierarchical block instance: I393(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i393;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 367)
   (short_msg "WARNING : Hierarchical block page3_i130 is empty")
   (long_msg "
Hierarchical block instance: I130(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i130;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 368)
   (short_msg "WARNING : Hierarchical block page3_i52 is empty")
   (long_msg "
Hierarchical block instance: I52(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i52;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 369)
   (short_msg "WARNING : Hierarchical block page2_i385 is empty")
   (long_msg "
Hierarchical block instance: I385(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i385;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 370)
   (short_msg "WARNING : Hierarchical block page3_i161 is empty")
   (long_msg "
Hierarchical block instance: I161(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i161;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 371)
   (short_msg "WARNING : Hierarchical block page2_i590 is empty")
   (long_msg "
Hierarchical block instance: I590(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i590;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 372)
   (short_msg "INFO : Unconnected pin 'd_OP_1_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 373)
   (short_msg "INFO : Unconnected pin 'd_OP_7_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 374)
   (short_msg "INFO : Unconnected pin 'd_OP_6_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 375)
   (short_msg "INFO : Unconnected pin 'ceo_STAR_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: ceo_STAR_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.ceo_STAR_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 376)
   (short_msg "INFO : Unconnected pin 'd_OP_5_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 377)
   (short_msg "INFO : Unconnected pin 'd_OP_4_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 378)
   (short_msg "INFO : Unconnected pin 'd_OP_3_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 379)
   (short_msg "INFO : Unconnected pin 'd_OP_2_CP_' on hierarchical block page2_i427")
   (long_msg "
Instance: I427(page2)
Pin Name: d_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i427.d_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 380)
   (short_msg "WARNING : Hierarchical block page2_i427 is empty")
   (long_msg "
Hierarchical block instance: I427(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i427;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 381)
   (short_msg "INFO : Unconnected pin 'x2' on hierarchical block page2_i379")
   (long_msg "
Instance: I379(page2)
Pin Name: x2

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i379.x2;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 382)
   (short_msg "WARNING : Hierarchical block page2_i379 is empty")
   (long_msg "
Hierarchical block instance: I379(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i379;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 383)
   (short_msg "WARNING : Hierarchical block page2_i291 is empty")
   (long_msg "
Hierarchical block instance: I291(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i291;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 384)
   (short_msg "WARNING : Hierarchical block page3_i116 is empty")
   (long_msg "
Hierarchical block instance: I116(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i116;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 385)
   (short_msg "WARNING : Hierarchical block page3_i67 is empty")
   (long_msg "
Hierarchical block instance: I67(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i67;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 386)
   (short_msg "WARNING : Hierarchical block page2_i522 is empty")
   (long_msg "
Hierarchical block instance: I522(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i522;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 387)
   (short_msg "WARNING : Hierarchical block page3_i121 is empty")
   (long_msg "
Hierarchical block instance: I121(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i121;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 388)
   (short_msg "WARNING : Hierarchical block page3_i89 is empty")
   (long_msg "
Hierarchical block instance: I89(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i89;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 389)
   (short_msg "WARNING : Hierarchical block page2_i520 is empty")
   (long_msg "
Hierarchical block instance: I520(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i520;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 390)
   (short_msg "WARNING : Hierarchical block page3_i189 is empty")
   (long_msg "
Hierarchical block instance: I189(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i189;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 391)
   (short_msg "WARNING : Hierarchical block page3_i111 is empty")
   (long_msg "
Hierarchical block instance: I111(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i111;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 392)
   (short_msg "WARNING : Hierarchical block page3_i1 is empty")
   (long_msg "
Hierarchical block instance: I1(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i1;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 393)
   (short_msg "WARNING : Hierarchical block page2_i504 is empty")
   (long_msg "
Hierarchical block instance: I504(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i504;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 394)
   (short_msg "WARNING : Hierarchical block page2_i437 is empty")
   (long_msg "
Hierarchical block instance: I437(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i437;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 395)
   (short_msg "WARNING : Hierarchical block page3_i107 is empty")
   (long_msg "
Hierarchical block instance: I107(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i107;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 396)
   (short_msg "INFO : Unconnected pin 'd_OP_13_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 397)
   (short_msg "INFO : Unconnected pin 'b_OP_4_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 398)
   (short_msg "INFO : Unconnected pin 'c_OP_21_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 399)
   (short_msg "INFO : Unconnected pin 'b_OP_13_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 400)
   (short_msg "INFO : Unconnected pin 'c_OP_6_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 401)
   (short_msg "INFO : Unconnected pin 'a_OP_21_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 402)
   (short_msg "INFO : Unconnected pin 'c_OP_16_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 403)
   (short_msg "INFO : Unconnected pin 'a_OP_8_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 404)
   (short_msg "INFO : Unconnected pin 'd_OP_8_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 405)
   (short_msg "INFO : Unconnected pin 'a_OP_16_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 406)
   (short_msg "INFO : Unconnected pin 'd_OP_19_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 407)
   (short_msg "INFO : Unconnected pin 'b_OP_19_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 408)
   (short_msg "INFO : Unconnected pin 'a_OP_1_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 409)
   (short_msg "INFO : Unconnected pin 'd_OP_1_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 410)
   (short_msg "INFO : Unconnected pin 'd_OP_12_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 411)
   (short_msg "INFO : Unconnected pin 'b_OP_3_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 412)
   (short_msg "INFO : Unconnected pin 'c_OP_20_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 413)
   (short_msg "INFO : Unconnected pin 'b_OP_12_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 414)
   (short_msg "INFO : Unconnected pin 'c_OP_5_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 415)
   (short_msg "INFO : Unconnected pin 'a_OP_20_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 416)
   (short_msg "INFO : Unconnected pin 'c_OP_15_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 417)
   (short_msg "INFO : Unconnected pin 'a_OP_7_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 418)
   (short_msg "INFO : Unconnected pin 'd_OP_7_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 419)
   (short_msg "INFO : Unconnected pin 'a_OP_15_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 420)
   (short_msg "INFO : Unconnected pin 'd_OP_18_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 421)
   (short_msg "INFO : Unconnected pin 'b_OP_9_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 422)
   (short_msg "INFO : Unconnected pin 'b_OP_18_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 423)
   (short_msg "INFO : Unconnected pin 'd_OP_11_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 424)
   (short_msg "INFO : Unconnected pin 'b_OP_2_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 425)
   (short_msg "INFO : Unconnected pin 'b_OP_11_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 426)
   (short_msg "INFO : Unconnected pin 'c_OP_4_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 427)
   (short_msg "INFO : Unconnected pin 'd_OP_22_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 428)
   (short_msg "INFO : Unconnected pin 'c_OP_14_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 429)
   (short_msg "INFO : Unconnected pin 'a_OP_6_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 430)
   (short_msg "INFO : Unconnected pin 'd_OP_6_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 431)
   (short_msg "INFO : Unconnected pin 'b_OP_22_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 432)
   (short_msg "INFO : Unconnected pin 'a_OP_14_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 433)
   (short_msg "INFO : Unconnected pin 'd_OP_17_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 434)
   (short_msg "INFO : Unconnected pin 'b_OP_8_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 435)
   (short_msg "INFO : Unconnected pin 'b_OP_17_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 436)
   (short_msg "INFO : Unconnected pin 'd_OP_10_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 437)
   (short_msg "INFO : Unconnected pin 'b_OP_1_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 438)
   (short_msg "INFO : Unconnected pin 'b_OP_10_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 439)
   (short_msg "INFO : Unconnected pin 'c_OP_3_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 440)
   (short_msg "INFO : Unconnected pin 'd_OP_21_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 441)
   (short_msg "INFO : Unconnected pin 'c_OP_13_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 442)
   (short_msg "INFO : Unconnected pin 'a_OP_5_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 443)
   (short_msg "INFO : Unconnected pin 'd_OP_5_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 444)
   (short_msg "INFO : Unconnected pin 'b_OP_21_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 445)
   (short_msg "INFO : Unconnected pin 'a_OP_13_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 446)
   (short_msg "INFO : Unconnected pin 'd_OP_16_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 447)
   (short_msg "INFO : Unconnected pin 'b_OP_7_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 448)
   (short_msg "INFO : Unconnected pin 'b_OP_16_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 449)
   (short_msg "INFO : Unconnected pin 'c_OP_9_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 450)
   (short_msg "INFO : Unconnected pin 'c_OP_19_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 451)
   (short_msg "INFO : Unconnected pin 'a_OP_19_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 452)
   (short_msg "INFO : Unconnected pin 'c_OP_2_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 453)
   (short_msg "INFO : Unconnected pin 'd_OP_20_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 454)
   (short_msg "INFO : Unconnected pin 'c_OP_12_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 455)
   (short_msg "INFO : Unconnected pin 'a_OP_4_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 456)
   (short_msg "INFO : Unconnected pin 'd_OP_4_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 457)
   (short_msg "INFO : Unconnected pin 'b_OP_20_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 458)
   (short_msg "INFO : Unconnected pin 'a_OP_12_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 459)
   (short_msg "INFO : Unconnected pin 'd_OP_15_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 460)
   (short_msg "INFO : Unconnected pin 'b_OP_6_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 461)
   (short_msg "INFO : Unconnected pin 'b_OP_15_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 462)
   (short_msg "INFO : Unconnected pin 'c_OP_8_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 463)
   (short_msg "INFO : Unconnected pin 'c_OP_18_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 464)
   (short_msg "INFO : Unconnected pin 'a_OP_18_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 465)
   (short_msg "INFO : Unconnected pin 'c_OP_1_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 466)
   (short_msg "INFO : Unconnected pin 'c_OP_11_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 467)
   (short_msg "INFO : Unconnected pin 'a_OP_3_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 468)
   (short_msg "INFO : Unconnected pin 'd_OP_3_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 469)
   (short_msg "INFO : Unconnected pin 'a_OP_11_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 470)
   (short_msg "INFO : Unconnected pin 'd_OP_14_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 471)
   (short_msg "INFO : Unconnected pin 'b_OP_5_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 472)
   (short_msg "INFO : Unconnected pin 'c_OP_22_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 473)
   (short_msg "INFO : Unconnected pin 'b_OP_14_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: b_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.b_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 474)
   (short_msg "INFO : Unconnected pin 'c_OP_7_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 475)
   (short_msg "INFO : Unconnected pin 'a_OP_22_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 476)
   (short_msg "INFO : Unconnected pin 'c_OP_17_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 477)
   (short_msg "INFO : Unconnected pin 'a_OP_9_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 478)
   (short_msg "INFO : Unconnected pin 'd_OP_9_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 479)
   (short_msg "INFO : Unconnected pin 'a_OP_17_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 480)
   (short_msg "INFO : Unconnected pin 'c_OP_10_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: c_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.c_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 481)
   (short_msg "INFO : Unconnected pin 'a_OP_2_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 482)
   (short_msg "INFO : Unconnected pin 'd_OP_2_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: d_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.d_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 483)
   (short_msg "INFO : Unconnected pin 'a_OP_10_CP_' on hierarchical block page3_i179")
   (long_msg "
Instance: I179(page3)
Pin Name: a_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i179.a_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 484)
   (short_msg "WARNING : Hierarchical block page3_i179 is empty")
   (long_msg "
Hierarchical block instance: I179(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i179;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 485)
   (short_msg "WARNING : Hierarchical block page3_i126 is empty")
   (long_msg "
Hierarchical block instance: I126(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i126;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 486)
   (short_msg "WARNING : Hierarchical block page3_i131 is empty")
   (long_msg "
Hierarchical block instance: I131(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i131;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 487)
   (short_msg "WARNING : Hierarchical block page3_i86 is empty")
   (long_msg "
Hierarchical block instance: I86(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i86;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 488)
   (short_msg "WARNING : Hierarchical block page3_i75 is empty")
   (long_msg "
Hierarchical block instance: I75(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i75;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 489)
   (short_msg "WARNING : Hierarchical block page2_i586 is empty")
   (long_msg "
Hierarchical block instance: I586(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i586;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 490)
   (short_msg "WARNING : Hierarchical block page3_i76 is empty")
   (long_msg "
Hierarchical block instance: I76(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i76;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 491)
   (short_msg "WARNING : Hierarchical block page3_i49 is empty")
   (long_msg "
Hierarchical block instance: I49(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i49;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 492)
   (short_msg "WARNING : Hierarchical block page2_i514 is empty")
   (long_msg "
Hierarchical block instance: I514(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i514;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 493)
   (short_msg "WARNING : Hierarchical block page2_i389 is empty")
   (long_msg "
Hierarchical block instance: I389(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i389;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 494)
   (short_msg "WARNING : Hierarchical block page3_i117 is empty")
   (long_msg "
Hierarchical block instance: I117(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i117;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 495)
   (short_msg "INFO : Unconnected pin 'y2_STAR_' on hierarchical block page3_i98")
   (long_msg "
Instance: I98(page3)
Pin Name: y2_STAR_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i98.y2_STAR_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 496)
   (short_msg "WARNING : Hierarchical block page3_i98 is empty")
   (long_msg "
Hierarchical block instance: I98(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i98;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 497)
   (short_msg "WARNING : Hierarchical block page3_i81 is empty")
   (long_msg "
Hierarchical block instance: I81(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i81;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 498)
   (short_msg "INFO : Unconnected pin 'n.c.' on hierarchical block page3_i44")
   (long_msg "
Instance: I44(page3)
Pin Name: n.c.

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i44.n.c.;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 499)
   (short_msg "WARNING : Hierarchical block page3_i44 is empty")
   (long_msg "
Hierarchical block instance: I44(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i44;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 500)
   (short_msg "WARNING : Hierarchical block page2_i523 is empty")
   (long_msg "
Hierarchical block instance: I523(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i523;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 501)
   (short_msg "WARNING : Hierarchical block page2_i494 is empty")
   (long_msg "
Hierarchical block instance: I494(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i494;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 502)
   (short_msg "WARNING : Hierarchical block page3_i112 is empty")
   (long_msg "
Hierarchical block instance: I112(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i112;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 503)
   (short_msg "WARNING : Hierarchical block page3_i92 is empty")
   (long_msg "
Hierarchical block instance: I92(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i92;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 504)
   (short_msg "WARNING : Hierarchical block page2_i505 is empty")
   (long_msg "
Hierarchical block instance: I505(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i505;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 505)
   (short_msg "WARNING : Hierarchical block page2_i436 is empty")
   (long_msg "
Hierarchical block instance: I436(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i436;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 506)
   (short_msg "WARNING : Hierarchical block page2_i426 is empty")
   (long_msg "
Hierarchical block instance: I426(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i426;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 507)
   (short_msg "WARNING : Hierarchical block page3_i127 is empty")
   (long_msg "
Hierarchical block instance: I127(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i127;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 508)
   (short_msg "WARNING : Hierarchical block page3_i95 is empty")
   (long_msg "
Hierarchical block instance: I95(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i95;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 509)
   (short_msg "WARNING : Hierarchical block page2_i591 is empty")
   (long_msg "
Hierarchical block instance: I591(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i591;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 510)
   (short_msg "WARNING : Hierarchical block page2_i549 is empty")
   (long_msg "
Hierarchical block instance: I549(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i549;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 511)
   (short_msg "WARNING : Hierarchical block page2_i545 is empty")
   (long_msg "
Hierarchical block instance: I545(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i545;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 512)
   (short_msg "WARNING : Hierarchical block page2_i524 is empty")
   (long_msg "
Hierarchical block instance: I524(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i524;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 513)
   (short_msg "WARNING : Hierarchical block page2_i325 is empty")
   (long_msg "
Hierarchical block instance: I325(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i325;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 514)
   (short_msg "WARNING : Hierarchical block page3_i122 is empty")
   (long_msg "
Hierarchical block instance: I122(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i122;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 515)
   (short_msg "WARNING : Hierarchical block page3_i77 is empty")
   (long_msg "
Hierarchical block instance: I77(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i77;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 516)
   (short_msg "WARNING : Hierarchical block page2_i602 is empty")
   (long_msg "
Hierarchical block instance: I602(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i602;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 517)
   (short_msg "WARNING : Hierarchical block page3_i113 is empty")
   (long_msg "
Hierarchical block instance: I113(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i113;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 518)
   (short_msg "WARNING : Hierarchical block page2_i407 is empty")
   (long_msg "
Hierarchical block instance: I407(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i407;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 519)
   (short_msg "WARNING : Hierarchical block page2_i79 is empty")
   (long_msg "
Hierarchical block instance: I79(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i79;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 520)
   (short_msg "WARNING : Hierarchical block page3_i190 is empty")
   (long_msg "
Hierarchical block instance: I190(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i190;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 521)
   (short_msg "WARNING : Hierarchical block page3_i103 is empty")
   (long_msg "
Hierarchical block instance: I103(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i103;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 522)
   (short_msg "WARNING : Hierarchical block page3_i87 is empty")
   (long_msg "
Hierarchical block instance: I87(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i87;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 523)
   (short_msg "WARNING : Hierarchical block page3_i40 is empty")
   (long_msg "
Hierarchical block instance: I40(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i40;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 524)
   (short_msg "WARNING : Hierarchical block page2_i587 is empty")
   (long_msg "
Hierarchical block instance: I587(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i587;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 525)
   (short_msg "WARNING : Hierarchical block page2_i583 is empty")
   (long_msg "
Hierarchical block instance: I583(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i583;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 526)
   (short_msg "WARNING : Hierarchical block page2_i554 is empty")
   (long_msg "
Hierarchical block instance: I554(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i554;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 527)
   (short_msg "WARNING : Hierarchical block page3_i128 is empty")
   (long_msg "
Hierarchical block instance: I128(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i128;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 528)
   (short_msg "WARNING : Hierarchical block page2_i592 is empty")
   (long_msg "
Hierarchical block instance: I592(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i592;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 529)
   (short_msg "WARNING : Hierarchical block page3_i118 is empty")
   (long_msg "
Hierarchical block instance: I118(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i118;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 530)
   (short_msg "WARNING : Hierarchical block page3_i82 is empty")
   (long_msg "
Hierarchical block instance: I82(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i82;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 531)
   (short_msg "INFO : Unconnected pin 'n.c.' on hierarchical block page3_i45")
   (long_msg "
Instance: I45(page3)
Pin Name: n.c.

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i45.n.c.;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 532)
   (short_msg "WARNING : Hierarchical block page3_i45 is empty")
   (long_msg "
Hierarchical block instance: I45(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i45;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 533)
   (short_msg "WARNING : Hierarchical block page3_i123 is empty")
   (long_msg "
Hierarchical block instance: I123(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i123;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 534)
   (short_msg "WARNING : Hierarchical block page3_i56 is empty")
   (long_msg "
Hierarchical block instance: I56(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i56;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 535)
   (short_msg "WARNING : Hierarchical block page3_i93 is empty")
   (long_msg "
Hierarchical block instance: I93(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i93;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 536)
   (short_msg "WARNING : Hierarchical block page3_i64 is empty")
   (long_msg "
Hierarchical block instance: I64(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i64;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 537)
   (short_msg "WARNING : Hierarchical block page3_i43 is empty")
   (long_msg "
Hierarchical block instance: I43(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i43;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 538)
   (short_msg "WARNING : Hierarchical block page3_i109 is empty")
   (long_msg "
Hierarchical block instance: I109(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i109;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 539)
   (short_msg "WARNING : Hierarchical block page2_i511 is empty")
   (long_msg "
Hierarchical block instance: I511(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i511;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 540)
   (short_msg "WARNING : Hierarchical block page2_i486 is empty")
   (long_msg "
Hierarchical block instance: I486(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i486;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 541)
   (short_msg "WARNING : Hierarchical block page3_i83 is empty")
   (long_msg "
Hierarchical block instance: I83(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i83;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 542)
   (short_msg "WARNING : Hierarchical block page3_i191 is empty")
   (long_msg "
Hierarchical block instance: I191(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i191;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 543)
   (short_msg "WARNING : Hierarchical block page2_i588 is empty")
   (long_msg "
Hierarchical block instance: I588(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i588;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 544)
   (short_msg "WARNING : Hierarchical block page2_i552 is empty")
   (long_msg "
Hierarchical block instance: I552(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i552;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 545)
   (short_msg "WARNING : Hierarchical block page2_i419 is empty")
   (long_msg "
Hierarchical block instance: I419(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i419;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 546)
   (short_msg "WARNING : Hierarchical block page3_i159 is empty")
   (long_msg "
Hierarchical block instance: I159(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i159;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 547)
   (short_msg "WARNING : Hierarchical block page3_i119 is empty")
   (long_msg "
Hierarchical block instance: I119(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i119;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 548)
   (short_msg "WARNING : Hierarchical block page3_i100 is empty")
   (long_msg "
Hierarchical block instance: I100(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i100;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 549)
   (short_msg "WARNING : Hierarchical block page2_i496 is empty")
   (long_msg "
Hierarchical block instance: I496(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i496;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 550)
   (short_msg "INFO : Unconnected pin 'e_OP_21_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 551)
   (short_msg "INFO : Unconnected pin 'd_OP_13_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 552)
   (short_msg "INFO : Unconnected pin 'b_OP_4_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 553)
   (short_msg "INFO : Unconnected pin 'e_OP_4_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 554)
   (short_msg "INFO : Unconnected pin 'b_OP_13_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 555)
   (short_msg "INFO : Unconnected pin 'e_OP_16_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 556)
   (short_msg "INFO : Unconnected pin 'a_OP_21_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 557)
   (short_msg "INFO : Unconnected pin 'f_OP_6_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 558)
   (short_msg "INFO : Unconnected pin 'a_OP_8_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 559)
   (short_msg "INFO : Unconnected pin 'f_OP_19_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 560)
   (short_msg "INFO : Unconnected pin 'd_OP_8_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 561)
   (short_msg "INFO : Unconnected pin 'a_OP_16_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 562)
   (short_msg "INFO : Unconnected pin 'd_OP_19_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 563)
   (short_msg "INFO : Unconnected pin 'b_OP_19_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 564)
   (short_msg "INFO : Unconnected pin 'a_OP_1_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 565)
   (short_msg "INFO : Unconnected pin 'f_OP_12_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 566)
   (short_msg "INFO : Unconnected pin 'd_OP_1_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 567)
   (short_msg "INFO : Unconnected pin 'e_OP_20_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 568)
   (short_msg "INFO : Unconnected pin 'd_OP_12_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 569)
   (short_msg "INFO : Unconnected pin 'b_OP_3_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 570)
   (short_msg "INFO : Unconnected pin 'e_OP_3_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 571)
   (short_msg "INFO : Unconnected pin 'b_OP_12_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 572)
   (short_msg "INFO : Unconnected pin 'e_OP_15_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 573)
   (short_msg "INFO : Unconnected pin 'a_OP_20_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 574)
   (short_msg "INFO : Unconnected pin 'f_OP_5_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 575)
   (short_msg "INFO : Unconnected pin 'a_OP_7_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 576)
   (short_msg "INFO : Unconnected pin 'f_OP_18_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 577)
   (short_msg "INFO : Unconnected pin 'd_OP_7_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 578)
   (short_msg "INFO : Unconnected pin 'a_OP_15_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 579)
   (short_msg "INFO : Unconnected pin 'd_OP_18_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 580)
   (short_msg "INFO : Unconnected pin 'b_OP_9_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 581)
   (short_msg "INFO : Unconnected pin 'e_OP_9_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 582)
   (short_msg "INFO : Unconnected pin 'b_OP_18_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 583)
   (short_msg "INFO : Unconnected pin 'f_OP_11_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 584)
   (short_msg "INFO : Unconnected pin 'd_OP_11_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 585)
   (short_msg "INFO : Unconnected pin 'b_OP_2_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 586)
   (short_msg "INFO : Unconnected pin 'e_OP_2_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 587)
   (short_msg "INFO : Unconnected pin 'b_OP_11_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 588)
   (short_msg "INFO : Unconnected pin 'f_OP_22_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 589)
   (short_msg "INFO : Unconnected pin 'e_OP_14_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 590)
   (short_msg "INFO : Unconnected pin 'f_OP_4_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 591)
   (short_msg "INFO : Unconnected pin 'd_OP_22_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 592)
   (short_msg "INFO : Unconnected pin 'a_OP_6_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 593)
   (short_msg "INFO : Unconnected pin 'f_OP_17_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 594)
   (short_msg "INFO : Unconnected pin 'd_OP_6_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 595)
   (short_msg "INFO : Unconnected pin 'b_OP_22_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 596)
   (short_msg "INFO : Unconnected pin 'a_OP_14_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 597)
   (short_msg "INFO : Unconnected pin 'd_OP_17_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 598)
   (short_msg "INFO : Unconnected pin 'b_OP_8_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 599)
   (short_msg "INFO : Unconnected pin 'e_OP_8_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 600)
   (short_msg "INFO : Unconnected pin 'b_OP_17_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 601)
   (short_msg "INFO : Unconnected pin 'f_OP_10_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 602)
   (short_msg "INFO : Unconnected pin 'd_OP_10_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 603)
   (short_msg "INFO : Unconnected pin 'b_OP_1_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 604)
   (short_msg "INFO : Unconnected pin 'e_OP_1_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 605)
   (short_msg "INFO : Unconnected pin 'b_OP_10_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 606)
   (short_msg "INFO : Unconnected pin 'f_OP_21_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 607)
   (short_msg "INFO : Unconnected pin 'e_OP_13_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 608)
   (short_msg "INFO : Unconnected pin 'f_OP_3_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 609)
   (short_msg "INFO : Unconnected pin 'd_OP_21_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 610)
   (short_msg "INFO : Unconnected pin 'a_OP_5_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 611)
   (short_msg "INFO : Unconnected pin 'f_OP_16_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 612)
   (short_msg "INFO : Unconnected pin 'd_OP_5_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 613)
   (short_msg "INFO : Unconnected pin 'b_OP_21_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_21_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_21_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 614)
   (short_msg "INFO : Unconnected pin 'a_OP_13_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 615)
   (short_msg "INFO : Unconnected pin 'd_OP_16_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 616)
   (short_msg "INFO : Unconnected pin 'b_OP_7_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 617)
   (short_msg "INFO : Unconnected pin 'e_OP_7_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 618)
   (short_msg "INFO : Unconnected pin 'b_OP_16_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_16_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_16_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 619)
   (short_msg "INFO : Unconnected pin 'e_OP_19_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 620)
   (short_msg "INFO : Unconnected pin 'f_OP_9_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 621)
   (short_msg "INFO : Unconnected pin 'a_OP_19_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_19_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_19_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 622)
   (short_msg "INFO : Unconnected pin 'f_OP_20_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 623)
   (short_msg "INFO : Unconnected pin 'e_OP_12_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 624)
   (short_msg "INFO : Unconnected pin 'f_OP_2_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 625)
   (short_msg "INFO : Unconnected pin 'd_OP_20_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 626)
   (short_msg "INFO : Unconnected pin 'a_OP_4_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 627)
   (short_msg "INFO : Unconnected pin 'f_OP_15_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 628)
   (short_msg "INFO : Unconnected pin 'd_OP_4_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_4_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_4_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 629)
   (short_msg "INFO : Unconnected pin 'b_OP_20_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_20_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_20_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 630)
   (short_msg "INFO : Unconnected pin 'a_OP_12_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_12_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_12_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 631)
   (short_msg "INFO : Unconnected pin 'd_OP_15_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 632)
   (short_msg "INFO : Unconnected pin 'b_OP_6_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 633)
   (short_msg "INFO : Unconnected pin 'e_OP_6_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_6_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_6_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 634)
   (short_msg "INFO : Unconnected pin 'b_OP_15_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_15_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_15_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 635)
   (short_msg "INFO : Unconnected pin 'e_OP_18_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 636)
   (short_msg "INFO : Unconnected pin 'f_OP_8_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_8_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_8_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 637)
   (short_msg "INFO : Unconnected pin 'a_OP_18_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_18_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_18_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 638)
   (short_msg "INFO : Unconnected pin 'e_OP_11_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 639)
   (short_msg "INFO : Unconnected pin 'f_OP_1_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_1_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_1_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 640)
   (short_msg "INFO : Unconnected pin 'a_OP_3_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 641)
   (short_msg "INFO : Unconnected pin 'f_OP_14_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 642)
   (short_msg "INFO : Unconnected pin 'd_OP_3_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_3_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_3_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 643)
   (short_msg "INFO : Unconnected pin 'a_OP_11_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_11_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_11_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 644)
   (short_msg "INFO : Unconnected pin 'e_OP_22_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 645)
   (short_msg "INFO : Unconnected pin 'd_OP_14_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 646)
   (short_msg "INFO : Unconnected pin 'b_OP_5_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 647)
   (short_msg "INFO : Unconnected pin 'e_OP_5_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_5_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_5_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 648)
   (short_msg "INFO : Unconnected pin 'b_OP_14_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: b_OP_14_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.b_OP_14_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 649)
   (short_msg "INFO : Unconnected pin 'e_OP_17_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 650)
   (short_msg "INFO : Unconnected pin 'a_OP_22_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_22_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_22_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 651)
   (short_msg "INFO : Unconnected pin 'f_OP_7_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_7_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_7_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 652)
   (short_msg "INFO : Unconnected pin 'a_OP_9_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 653)
   (short_msg "INFO : Unconnected pin 'd_OP_9_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_9_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_9_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 654)
   (short_msg "INFO : Unconnected pin 'a_OP_17_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_17_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_17_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 655)
   (short_msg "INFO : Unconnected pin 'e_OP_10_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: e_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.e_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 656)
   (short_msg "INFO : Unconnected pin 'a_OP_2_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 657)
   (short_msg "INFO : Unconnected pin 'f_OP_13_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: f_OP_13_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.f_OP_13_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 658)
   (short_msg "INFO : Unconnected pin 'd_OP_2_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: d_OP_2_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.d_OP_2_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 10)
   (error_num 659)
   (short_msg "INFO : Unconnected pin 'a_OP_10_CP_' on hierarchical block page3_i175")
   (long_msg "
Instance: I175(page3)
Pin Name: a_OP_10_CP_

The specified pin is an isolated pin. This might cause topological 
error during simulation.

INFORMATION: Connection of a pin can be established in either of the following ways:
1. Connect it through a wire to another signal/pin.
2. Attach FLOAT property with value 'e', 'r', 'n' or 'u' to specify:
   'e' : looks for 'Net_Name' property for the name of net to be connected to this pin.
   'r' : attached a large resistor (1/GMIN) to the pin and grounds the resistor. Note
	   however that this net will need to be connected to another pin/signal for
	   simulation to proceed correctly.
   'n' : generates a unique net name, and attaches it to the pin.
   'u' : if this pin should not be modeled in the netlist.

")
   (location
    (
     (object_kind "InstancePin")
     (canonical_name "_!drawerror pin i175.a_OP_10_CP_;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 660)
   (short_msg "WARNING : Hierarchical block page3_i175 is empty")
   (long_msg "
Hierarchical block instance: I175(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i175;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 661)
   (short_msg "WARNING : Hierarchical block page3_i114 is empty")
   (long_msg "
Hierarchical block instance: I114(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i114;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 662)
   (short_msg "WARNING : Hierarchical block page2_i422 is empty")
   (long_msg "
Hierarchical block instance: I422(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i422;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 663)
   (short_msg "WARNING : Hierarchical block page2_i421 is empty")
   (long_msg "
Hierarchical block instance: I421(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i421;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 664)
   (short_msg "WARNING : Hierarchical block page3_i129 is empty")
   (long_msg "
Hierarchical block instance: I129(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i129;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 665)
   (short_msg "WARNING : Hierarchical block page3_i62 is empty")
   (long_msg "
Hierarchical block instance: I62(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i62;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 666)
   (short_msg "WARNING : Hierarchical block page2_i593 is empty")
   (long_msg "
Hierarchical block instance: I593(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i593;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 667)
   (short_msg "WARNING : Hierarchical block page2_i378 is empty")
   (long_msg "
Hierarchical block instance: I378(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i378;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 668)
   (short_msg "WARNING : Hierarchical block page3_i192 is empty")
   (long_msg "
Hierarchical block instance: I192(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i192;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 669)
   (short_msg "WARNING : Hierarchical block page2_i424 is empty")
   (long_msg "
Hierarchical block instance: I424(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i424;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 670)
   (short_msg "WARNING : Hierarchical block page3_i124 is empty")
   (long_msg "
Hierarchical block instance: I124(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i124;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 671)
   (short_msg "WARNING : Hierarchical block page2_i423 is empty")
   (long_msg "
Hierarchical block instance: I423(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i423;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 672)
   (short_msg "WARNING : Hierarchical block page3_i38 is empty")
   (long_msg "
Hierarchical block instance: I38(page3)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i38;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page3")
    )
   )
  )
  (
   (tool "Concept")
   (class "SCHEMATIC")
   (severity 30)
   (error_num 673)
   (short_msg "WARNING : Hierarchical block page2_i512 is empty")
   (long_msg "
Hierarchical block instance: I512(page2)

This hierarchical block is empty. That is, it does not contain any circuitry
inside it. This warning acts only as a notification to the user.

In order to get rid of this warning, you should define some circuitry inside
this block.
")
   (location
    (
     (object_kind "Instance")
     (canonical_name "_!drawerror inst i512;")
     (parent_canonical_name "unused")
     (drawing_name "@most3_lib.sint(sch_1):page2")
    )
   )
  )
 )
)
